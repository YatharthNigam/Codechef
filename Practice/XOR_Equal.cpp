#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve()
{
    int n, x;
    cin >> n >> x;
    vector<int>v(n);
    vector<int>temp(n);
    for(int i = 0; i < n; i++)
     {
        cin>>v[i];
        temp[i] = v[i]^x;
     }
     for()    
}

int32_t main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}