#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve()
{
    int n;
    cin >> n;
    string s;
    cin >> s;
    for (int i = 0,j=0; i < s.length(); j++)
    {
        if(s.at[j]=='.')
    }
}

int32_t main()
{
    solve();
}
